using System.ServiceModel;
using DBEntity;
using Services.Base;

namespace Services.Physical.WarehouseOuts
{
    // NOTE: You can use the "Rename" command on the "Refactor" menu to change the interface name "IPSQuotaRelService" in both code and config file together.
    [ServiceContract]
    public interface IPSQuotaRelService : IService<PSQuotaRel>
    {
        
    }
}
